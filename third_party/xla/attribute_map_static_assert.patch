--- a/xla/python/ifrt/attribute_map.h
+++ b/xla/python/ifrt/attribute_map.h
@@ -35,6 +35,9 @@
 namespace xla {
 namespace ifrt {
 
+template <typename...>
+constexpr bool always_false = false;
+
 // Attribute map that contains UTF-8 keys and variant values.
 //
 // This class is thread-safe.
@@ -112,7 +115,7 @@
     } else if constexpr (std::is_same_v<T, float>) {
       return Get<T, FloatValue>(key);
     } else {
-      static_assert(false, "Unsupported type for AttributeMap::Get");
+      static_assert(always_false<T>, "Unsupported type for AttributeMap::Get");
     }
   }
 
@@ -133,7 +136,7 @@
     } else if constexpr (std::is_same_v<ValueType, float>) {
       map_.insert_or_assign(key, FloatValue(std::move(value)));
     } else {
-      static_assert(false, "Unsupported type for AttributeMap::Set");
+      static_assert(always_false<T>, "Unsupported type for AttributeMap::Set");
     }
     return absl::OkStatus();
   }
