# APPEND this target to tpu_raiden/benchmarks/BUILD (do not replace the file).
# Depends only on the existing :perf_core target + numpy, so it needs nothing
# beyond what the presubmit gate already pulls in.

py_binary(
    name = "canary_ab_dist",
    testonly = True,
    srcs = ["canary_ab_dist.py"],
    deps = [
        ":perf_core",
        "@pypi//numpy",
    ],
)
