# TPU Raiden Worklog

## 2026-07-20
* Created a NEW CL to trigger a legitimate BAP failure without the skip tag.
* Added 1s latency to Python `perf_core.py` to ensure BAP fails without breaking internal TAP unit tests.
